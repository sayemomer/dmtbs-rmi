package Server;

public class Server {
    public static void main(String[] args) throws Exception {
        ServerInstance SherbrookeServer = new ServerInstance("ATW");
        ServerInstance QuebecServer = new ServerInstance("VER");
        ServerInstance MontrealServer = new ServerInstance("OUT");
    }
}